# Contract-to-runtime reconciliation — 2026-08-25

## Current Kingdom Quickstart founding-cohort census — 2026-09-10

3078 production C# files; 3,109 cold-install files in the generated inventory;
436,999 physical lines, 1434 direct-XRL, zero cap failures. Exact digest
`3aa112eccece1111e1ac4800a827e5851722be7a9bf154ae2bf65f6b66f697b4`.
Quickstart's founding cohort is split at the reversibility boundary: one grant scope for four bodies
and all their gear, a publish that names the exact four, then a forward-only idempotent enrolment
under an append-only `Founding` reason. Old receipts keep their exact bytes and are terminal.
Each founder is counted into the shared origin tally by adding one under a durable identity-bound
obligation; mixed state is refused in the open rather than inferred from a counter other arrivals
move. The canonical gate compiled all four modes clean on these bytes, and the engine-free suites
passed 14,132 main / 5,286 Portable with zero skips; no native evidence binds them.

## Retained stockpile deposit custody census — 2026-09-09

3068 production C# files; 3,099 cold-install files in the generated inventory;
435,538 physical lines, 1429 direct-XRL, zero cap failures. Exact digest
`3cfe76c38704930c03d2923e400d05155cbcbf96b9ad8b31ad90304cc8fea6c0`.
A deposit that cannot prove where its bundle went now stops the whole delivery rather than making
the material a second time, and a body a callback is holding is never obliterated to resolve the
ambiguity. The law is engine-free and the GameObject seam is the only piece that touches one.
Roslyn 9.0.306 on Linux compiled the staged baseline (3064 sources) and compatibility (3068
sources) sets clean on these bytes, and the engine-free suites passed 14,061 main / 5,215 Portable
with zero skips; no dev-harness mode, Windows gate or native evidence binds them.

## Retained Kingdom Quickstart shelter-ingress census — 2026-09-09

3062 production C# files; 3,093 cold-install files in the generated inventory;
434,436 physical lines, 1425 direct-XRL, zero cap failures. Exact digest
`ff13330463a990edbef95c2ae35e0e552691f2f8e0f86a525dc873bd61f7c202`.
The camp bares each shelter lot's authored DoorToLane route as well as the lot, so the stake's
public-ingress preflight finds walkable ground on every profile rather than only on the dunes;
nothing here invents a completion and a route cell that cannot be bared still stops the bootstrap.
All four `Tools/gate.sh` modes (3050/3054/3204/3208) plus the installed-Hearthpyre source/ABI step
bound the delta's own pre-merge digest, and the six-profile native Quickstart boot matrix at seed
`#43101` passed on it together with `quickstart-save marsh yes` and its cold load. On the merged
tree Roslyn 9.0.306 on Linux compiled the staged baseline (3058 sources) and compatibility (3062
sources) sets clean; no dev-harness mode, Windows gate or native evidence binds these merged bytes.

## Retained city-sight over stockpile-capacity census — 2026-09-09

3061 production C# files; 3,092 cold-install files in the generated inventory;
434,296 physical lines, 1425 direct-XRL, zero cap failures. Exact digest
`7147169b7ccb8d2142d9791bd5faec8405eb305e33bca7a9b9feb9c3948c5a1e`.
City sight's projection is taken at the engine's own `Zone.Render` call, armed by a prefix on
`XRLCore.RenderBaseToBuffer`, behind every native second-pass contributor including `Blackout`.
The earlier seat — a Harmony postfix on the render dispatch's static entry — crashed the game in
unattended native runs and is now forbidden by the source contract. Merging its end-of-turn restore
backstop with the basin-capacity zone-activation guard put `Core/KingdomSystem.z20.Events.cs` at 305
physical lines, over the strict cap; the merge reflowed those two comment blocks wider, keeping
every word and engine citation and moving no code, and the shard is back at 299. Roslyn 9.0.306 on
Linux compiled the staged baseline (3057 sources) and compatibility (3061 sources) sets clean, run
against the installed managed assemblies rather than through `Tools/gate.sh`. No dev-harness mode,
Windows gate or native evidence binds this digest.

## Retained stockpile-capacity over first-basin water-store census — 2026-09-09

3059 production C# files; 3,090 cold-install files in the generated inventory;
433,954 physical lines, 1423 direct-XRL, zero cap failures. Exact digest
`5db8f7381ade172c6b0b34925a111f4d4c28f32da77cf0be266914aa53e77674`.
A dedicated stockpile declares its size in material units off a new blueprint tag; the two
counting paths never read it, so the settlement ledger and every purpose-local debit view agree
by construction and an over-cap standing save reads exactly what it read before. Only intake
refuses, and intake counts the outcome it proved rather than the call it made. No receipt phase,
wire, option, grant or verifier predicate changes. Roslyn 9.0.306 on Linux compiled the staged
baseline (3055 sources) and compatibility (3059 sources) sets clean.
No native, dev-harness or human semantic-review evidence binds this digest.

## Retained first-basin water-store over Quickstart tent-row census — 2026-09-09

3056 production C# files;3,087 cold-install files in the generated inventory;
433,308 physical lines,1421 direct-XRL,zero cap failures. Exact digest
`5160ed08e19734f315ebe8c7fe2ab4e5e7e1bb6bc632ae97a0c511d1f40cd325`.
The founding heart's first basin becomes the settlement's first water store: the relic slot stamps
the store dedication in code so the Debug gallery's copy never joins a settlement's accounts, and
capacity grows with the rung standing over it. A committed water receipt keeps its per-vessel hold
while its caller's compensation window is open, and every caller that can refund after its own
callbacks now opens that window before its commit and closes it in an enclosing finally. No receipt
phase, wire, option, grant or verifier predicate changes. Roslyn 9.0.306 on Linux compiled the
staged and dev-harness baseline and compatibility sets clean. No native, installed-ABI or human
semantic-review evidence binds this digest.

## Retained Kingdom Quickstart tent-row census — 2026-09-08

3053 production C# files;3,084 cold-install files in the generated inventory;
432,564 physical lines,1418 direct-XRL,zero cap failures. Exact digest
`f5571291a26a1bd1db608f0d1c3f79ed296569bddce4979208cb564f8c22df63`.
The quickstart bootstrap stakes two receiptless `tentrow` lots between the founding proof and the
receipt's first advance, one search per lot before either is staked; nothing there stamps
completion. Staged baseline (3049 sources) and staged compatibility (3053 sources) compiles bound
that digest, run on Linux with the SDK Roslyn against the installed managed assemblies rather than
through `Tools/gate.sh`; no dev-harness mode, Windows gate or native evidence bound it.

## Retained camp-guide topic census — 2026-09-08

3053 production C# files;3,084 cold-install files in the generated inventory;
432,178 physical lines,1417 direct-XRL,zero cap failures. Exact digest
`dcab3931d57df58aeaf3f0dee894acdec54d261a4e5f85d94cb369d8a1c73e96`.
The optional Quickstart guide gains five fixed information topics from one engine-free words
file; no receipt phase, wire, option, grant or verifier predicate changes, and existing saves
keep the one-node guide. It sits over the claimed-ground light below. Engine-free suites
pass13731 main/5109 Portable,zero skips on Linux .NET9.0.306, and Roslyn 9.0.306 on Linux
compiled the staged baseline (3048 sources) and compatibility (3052 sources) sets clean. No
native, dev-harness or human semantic-review evidence binds this digest.

## Retained claimed-ground light census — 2026-09-08

3051 production C# files;3,082 cold-install files in the generated inventory;
432,024 physical lines,1417 direct-XRL,zero cap failures. Exact digest
`fca337fa0b3642f0e4e485df3a015cbbd66b2a9c5d94cd8fd5d204c3e3f86f54`.
Presentation only: a mod-owned zone part raises the founder's own claimed zone to the torch
light tier per rendered frame and remembers its floor once per activation. No wire, no saved
field, no new completion authority. The staged baseline and compatibility sets compile clean under
Roslyn 9.0.306 on Linux; the dev-harness modes, the Windows gate and any native run did not.

## Retained master-growth resume census — 2026-09-07

3049 production C# files;3,080 cold-install files in the generated inventory;
431,893 physical lines,1415 direct-XRL,zero cap failures. Exact digest
`a3a9c8dd8ea36962475266e7005ccc6fcdd352b3bfd3d9c4675beb47b51be2b9`.
Complete canonical comparison and five-source review bind strict structure. Already-
published arrival work and open leases survive validated master resume; no paused backlog
is created. Native11624 and38 focused protocol cases pass. Full Windows1814 passes13715
main/5093 Portable,zero skips; four-mode53744 and repository71120 pass. Hosted integration
remains separate. No ordinary-play,save/load,new-public-release or broad Beta claim.

## Retained recovery census — 2026-09-07

3047 production C# files; 3,078 cold-install files in the generated inventory;
431,611 physical lines,1415 direct-XRL imports,zero cap failures. Exact inventory:
`6dbd94092f57eeb9b79f5ff169105fa5a7ab2cc6702480d99ba98c92be17bff9`.
Complete canonical comparison and scoped six-file semantic review bind strict structure.
Four-mode compile51408 and two native recovery cases51235 pass; full main suite27430
passes13661 main/5039 Portable cases, zero skips. Repository71541 passes501 tests and audits. This
does not sign ordinary play, save/load, paused/bound native guards or a new public release.

## Retained stock-hardening census — 2026-09-07

3046 production C# files; 3,077 cold-install files in the generated inventory;
431,441 physical lines,1414 direct-XRL imports and zero production cap failures. Production
digest: `9d9eb6416014c7257a26fa08178d8f738e44dd46a295ea3357f32b7739faf1b0`.
Compile73169 TERMINAL0 passes all four C#7.3 modes+ABI: ordinary3042/3046, developer3156/3160
with114 Harness. Managed47358 TERMINAL0 passes13625 Taf/5012 Portable, zero skips. Tools73664
TERMINAL0 passes501 tests and the exact structural `--release` gate passes. Native49420 TERMINAL0
passes16 synthetic creator/custody groups, including duplicate-child negatives inside both creator
groups and healthy fixture restoration; exact-owned PID5068 stop, root idle and no-allowance log
checks pass. Bounded stock proof is closed; component save/load and ordinary acceptance are not. This
is a mechanical inventory plus bounded two-production-file stock review, not a fresh
whole-tree review, installation, subscription or release claim. The six startup boot/save/
cold-load pairs and twelve strict result checks remain bound to earlier `7e3fb752…` bytes.
[Stock validation](/tmp/taf-quickstart-stock-validation.CM1dD9/README.md) retains the prototype's
missing-fixture failure; `docs/STATUS.md` owns current verification and remaining gates.

## Retained pre-upload census — 2026-09-06

Retained isolated draft census: 3045 production C# files; 3,076 cold-install files
in the generated inventory. No candidate installation, subscription, semantic review or release
is implied by those counts. `docs/STATUS.md` owns current verification and remaining gates.
The beta and older addenda below are retained chronological evidence, not current-inventory
signatures; their existing counts and reviewed hashes are deliberately unchanged.

Beta addendum 2026-09-05: 2951 production C# files; 2,982 cold-install files. Current semantic
review covers Quickstart exact custody, raid zone guards, canonical counts, and outbox quarantine
before diagnostics. Focused actual-lifecycle outbox tests pass34/34 in both projects, including
wire persistence. Separate native outbox persona passes6/6 actual sink cases with synthetic
interruptions; no ordinary-play/save-load claim. C#7.3 shadowing fixed without behavior changes.
Subsidence post-loop summary ordering is independently reviewed and passes13/13 eachproject;
full managed11231/portable2794,zero skips, including twelve native-fixture source contracts
and five Claude raid launch-order source/rule regressions. Raid projections/leases now publish
before creation, followed by per-actor create/place/activate through existing ResumeOpen.
Native multi-raider callback and replacement/interrupted-placement custody proof remain OPEN.
Tools338 passes literal/count expected-diagnostic, actual-shell lifecycle and Workshop regressions.
The new physical resident fixture executes actual Reckon; stage/elapsed checkpoint are seeded.
Fractional-step debt, committed-but-pending departures, inner-rung interruptions and ordinary
native/save-load proof remain open; this is not exactly-once recovery. STATUS.md owns final
native fixture evidence, including any refused log expectation and fresh-profile rerun.
Workshop uplift is canonical locally only: grouped player outcomes and accurate discovery tags,
with explicit Alpha/legacy/compatibility/backup caveats. Independent review and metadata/package
regressions cover both private/public identities and bootstrap absence. Package bind-alias
environment skip remains explicit. No new Alpha receipt, tag, upload or public-page change.
docs/STATUS.md owns current suite/compile/native evidence, not the dated audit.

> **Supersession banner — 2026-09-01.** This is a dated chronological reconciliation, not the live
> status ledger. Current runtime holds one seat plus two non-seat cities; third founding succeeds,
> fourth refuses, and the manifest offers both non-seat destinations. All accepted code/content
> lanes, including hosted arcology, are integrated at the frozen source snapshot. Native/human,
> current-preview, exact-inventory semantic-review, package, and Steam evidence remain open.
> `VISION.md`, `docs/STATUS.md`, `docs/V1-UNDEFERRAL.md`, and
> `BRIEF-IMPLEMENTATION-AUDIT.md` control current status where this dated body conflicts.

> **Current-truth addendum — 2026-09-01.** The historical checks below are the frozen 2026-08-25 snapshot, kept verbatim for the record; they are not live status.
> The current frozen census is 2945 production C# files and 2,976 cold-install files; 0 exceed 300 physical lines, 0 are exactly 300, so 0 fail strict; 0 exceed 1,000, 0 exceed 2,000, and 0 exceed 5,000.
> Latest retained managed evidence is 10,624/10,624 Qud-referenced/source and 2,325/2,325 portable, both predating the final market fan-in and requiring rerun; current documentation/static evidence is 300/300 Tools and 28/28 Art. The historical checkpoint passes 7,743 / 7,743 cases, including nine focused survey cases, only for its old bytes.
> See `docs/STATUS.md` and `docs/STRUCTURE.md` for the current receipts and open human gates.
> Historical generated-market/restock claims are superseded: current civic service requires one
> accepted staffed provider plus exact held office, opens empty, and uses physical native TradeUI
> `_stock` only. `ShopTier` is current standing/reach; automatic output/restock is zero. Personal
> completed/dormant traders survive civic loss/accession; prepared handoff endpoints alone pause
> succession.

Status: adversarial static pass, reconciled through the 2026-08-28 code-scope fan-in. This is not
release evidence.
Authoritative promises were read directly; `_notes/BRIEF-IMPLEMENTATION-AUDIT.md` and the historic
audits were used only as attack lists. Installed-engine, controller, visual, cold-save, compatibility,
and Workshop claims remain live gates.

Severity: P0 blocks the promised release; P1 is a concrete contract or proof gap that must close
before release; P2 is a truthful-documentation defect repaired in this pass.

## Severity matrix

| Severity | Contract | Reproduced runtime/content evidence | Required closure |
|---|---|---|---|
| P0 fixed, native proof open | Foundation requires unfounded and **master-off** end turns to be O(1), with zero allocation/RNG/zone access/scan/log/notification (`_notes/FOUNDATION-CONTRACT.md:608-620`), while read-only records and recovery remain accessible (`_notes/FOUNDATION-CONTRACT.md:904-921`). Its uninstall guidance explicitly depends on master-off (`_notes/FOUNDATION-CONTRACT.md:972-975`). | `Options.xml` now declares `r_TAF_OptionMaster`. `KingdomMasterRules` owns a persisted three-state latch and exactly-once resume token; `KingdomMaster` gates automatic wakes before zone/delegate work, keeps explicit read/recovery separate, and re-anchors city/module/object clocks before publishing resume. `KingdomMasterRulesTests` and `KingdomMasterAndCivicWrapperSourceTests` pin initialization, disable, same-tick transition, reload, producer, independent-part, and recovery boundaries. Baseline/compatibility compile is clean. | Run TESTING 7a-7d in Qud across end turn, object ticks, zone exchange, world map, save/cold-load, committed recovery, and all three cities; retain a log/scan receipt showing disabled wakes are physically inert and resume creates no backlog. |
| P0 fixed, native proof open | Civic targets globally mutated by the installed stack must use durable namespaced wrappers, starting with Campfire, Hookah, Bookshelf, and Torchpost (`_notes/FOUNDATION-CONTRACT.md:957-967`); unsafe catalogue targets must be wrapped or removed before construction (`_notes/FOUNDATION-CONTRACT.md:1037-1039`). | All shipped catalogue and architecture placements route through `r_KingdomCivicCampfire`, `r_KingdomCivicBookshelf`, `r_KingdomCivicTorchpost`, or `r_KingdomCivicHookah`. Each inherits vanilla art/function and removes only unsafe randomized/destructive/pickup/free-content behavior. A source census rejects the four raw roots anywhere in staged authored placement. | Cold-launch/save/reload with the installed merge stack; inspect final live parts and use each fixture through its ordinary vanilla interaction. Static inheritance and source checks cannot prove final merged capabilities. |
| P0 native evidence open | A threat separates grievance, rumor, delivered demand, response, and incident. Rumor has no deadline; demand time starts after acknowledgement; valid remote channels retain all four answers; channel loss pauses/redelivers (`_notes/FOUNDATION-CONTRACT.md:819-843`). The first complete slice is one Snapjaw grievance/profile, four answers, and recovery (`_notes/FOUNDATION-CONTRACT.md:1047-1048`). | The follow-up implementation now persists rumor, exact physical demand/revision, acknowledgement, paused redelivery, and lossless `ConfrontationReady`; preserves Talk/Tribute/Fight/Fortify remotely through the carried witness; adds the one-shot guarded-gate Snapjaw salt-road cause; and creates a bounded watch-disarray wound plus plain explicit-turn-in recovery quest only after proved store contact. Fortify now freezes exact defensive work IDs and globally exclusive resident semantic IDs, then re-proves the same work, post, binding, body, score, and crew before resolution; old unproved Fortified saves reopen safely. Unsupported profile/cause pairs fail before grievance creation. Raid-ledger v1/v2 bytes are pinned and migrate through the v3 framed codec; future framed payloads round-trip opaque. Portable transition/source coverage and native baseline/compatibility compilation pass. No current-revision live Qud encounter/cold-save receipt exists yet. | Execute the revised `TESTING.md` Pass 6/6b/10 protocol in Qud: witness rumor→object→acknowledgement, carried/local answers, loss/redelivery, remote deadline, natural source dedupe, fresh fortify recheck, exact objective contact, recovery accept/decline/turn-in, master/module pause, and cold-save interruptions. Retain Player.log/save/screenshots; this row remains a release blocker until that evidence is signed. |
| P0 fixed, native proof open | A carrier job is an exact timed multi-zone itinerary and must appear in every intermediate zone at the corresponding position (`_notes/BUILDING-CATALOGUE-BRIEF.md:625-640`; `_notes/LIVING-CITY-ARCHITECTURE.md:1021-1040`). The API claims the level-1 next-hop matrix feeds the itinerary (`docs/API.md:911-923`). | `KingdomDistanceRuntime` now measures real passable/paved cells and proved delve links into a bounded nonserialized sparse cache. `KingdomCentralLogistics` composes exact endpoint/next-hop routes, freezes every trip/leg and traversed paving discount, and owns scalar and manifest movement. Carry callers use `TryPlanSnapshot`; nearest-holder/no-nearer-holder proofs are live. Source/pure tests pin ground handoffs, sparse bounds, exact route composition, and central ownership. | Run the multi-zone/vertical porter and carry-sign protocol in Qud: follow through at least three zones in both directions, save/cold-load in each leg, obstruct an edge, change paving after freeze, and prove one body/cargo/receipt with no west fallback or teleport. |
| P0 fixed, native proof open | The carry-sign is a **crafted marker** planted on an owned pile or container anywhere; it hauls that designated property home (`_notes/BUILDING-CATALOGUE-BRIEF.md:102`). | `r_KingdomCarrySign` is craftable at tier 1 from bits `00`; merchant stock is an alternative. Carry schema v6 freezes a bounded mixed whole-object manifest from an exact cell or container, rejects unsafe/ambiguous/foreign cargo, publishes authority before callbacks, routes through central trips, and moves the same object references with `NoStack` rather than destroy/mint/material-bucket substitution. Source tests pin consent, ordering, mixed cargo, exact references, capacity, interruption, threat waiting, and craft wiring. | Native proof must cover mixed stacks/objects, cell and container sources, ownership/importance/equipment refusals, every receipt cut, obstruction/threat, save/cold-load, safe spill, and reference-identical delivery without stack merge. |
| P0 fixed, legacy-loss/native proof open | Citizenship is a removable overlay which preserves native allegiance, quests, conversations, ownership, temporary allegiance layers, follower leadership, and foreign lifecycle (`_notes/FOUNDATION-CONTRACT.md:75-76`). Culture and species must remain meaningful alongside civic identity (`_notes/BUILDING-CATALOGUE-BRIEF.md:980-994`). | `r_KingdomCitizenship` now owns one namespaced base-allegiance slot with exact prior/applied value, realm/settlement/body provenance, phase, removal, divergence, and death cleanup. Enrollment edits only that dictionary key through `GetBaseAllegiance`; it never assigns `Brain.Factions`, clears the graph, or touches temporary allegiance, leadership, conversation, quests, ownership, stats, or foreign parts. Exact graph/source tests pin apply/remove/reload/divergence and transaction snapshots. Old saves already overwritten by the historical setter are marked `LegacyPriorUnknown`; irrecoverable native mixtures are disclosed and never guessed. | In Qud test a mixed-faction recruit, follower, temporary allegiance above/below the base, save/cold-load, explicit departure, death, succession, foreign lifecycle part, and legacy receipt. Byte-/reference-compare every non-owned surface. |
| P0 fixed, native proof open | Every option is a state machine: disable banks no deadline or consequence and resume starts future work from now (`_notes/FOUNDATION-CONTRACT.md:77-78,931-936`; `_notes/SIMULATION-ARCHITECTURE.md:482-499`). A module switch owns only the behavior its label names. | `KingdomElapsedOptionRules` supplies one canonical persisted latch/epoch. Roads, subsidence, faith, growth admission, and the other elapsed owners observe transitions before scans/due work, anchor locally before publishing, cancel only unpaid pressure, preserve committed recovery, and consume the master resume token. Growth's option now owns admission rather than suppressing unrelated city functions. Pure/source fixtures cover initialization, disable, repeated wakes, master+module relatch, long disabled spans, due-1/due/due+1, save encoding, and future-only resume. | Execute the option matrix in Qud for every module and independent ticking object, including disabled due edges, master/module combinations, seat exchange, long world-map absence, save/cold-load at the owner-last publication cut, and committed recovery. No resume may immediately reward, convert, damage, remove, wear, or spawn. |
| P0 fixed, native proof open | Semantic content selection is counter-based, versioned, persisted before materialization, and never calls global `Stat.Random`, random collection helpers, no-argument `NameMaker`, or `PopulationManager.Roll*` (`_notes/FOUNDATION-CONTRACT.md:650-669`). | `KingdomSemanticSelectionRules` now canonicalizes merged direct-object weights, draws through `CounterRandom`, generates versioned Qud-literate names, and supplies one fixed cyclic coordinate probe. Growth-v3 candidates and archive-v11 persist the complete person plan before creation; v1/v2 candidates migrate once before materialization. Plain/notable guests, causal pilgrims, lodge creeds, legacy furnishings, commission fallback ground, and salvage sign-off no longer enter global RNG. Unsupported grouped/dynamic/call population rows fail closed; `PopulationManager` resolves merged data only and never chooses or generates. Pure/source fixtures pin merge order, cadence, retry, reload, migration, weights, and freeze order. | In current Qud, sample merged third-party settler/guest/furnishing rows, interrupt at every candidate/create/furnish cut, cold-save/reload, and prove identical blueprint/name/origin/creed/title/coordinate plus one object per authored roll. Retain logs/save/screenshots; native evidence remains release-blocking. |
| P0 repair in working tree; dense native proof open | One active-seat reconciliation classifies the zone once; all modules and reports consume and maintain that shared index, and no helper performs a second whole-zone scan (`_notes/FOUNDATION-CONTRACT.md:669-676,1065-1070`; `_notes/DECISIONS.md:38-43`). | `AttendSeatedSemantics` binds one `KingdomSurvey`. The survey now publishes named indexes for construction/plot/layout/crop/network/lab/visual roots, resident bodies, bindings, and transient jobs, and it observes committed additions/removals. Residents, construction presence, lab, networks, layout, plots, roads, crops, visual refresh, upgrade handover, and porter folds/sweeps/binding resolution consume those indexes during the bound pass. Duplicate resident/binding identity fails closed. Focused source tests pass; baseline/compatibility compile is clean. A complete reachable-call-graph scan and native counter receipt are still required before this P0 closes. | Finish the active-pass raw-scan audit, add one instrumented dense-zone receipt, then execute every semantic branch with construction/arrival/removal mutations. Prove exactly one classification, bounded exact-cell/object reproof only, and fresh later-lane index reads. |
| P0 fixed, native proof open | A declared weighted single-pick population must actually use Qud's `Style="pickone"`; otherwise `PopulationList.Generate` emits every row and `Weight` has no effect. Furnishing documentation explicitly promises one weighted pick per roll (`PopulationTables.xml:90-97`; `MODDING.md:515-518`). | All settler, guest, notable, and five furnishing populations supplied `Weight` but omitted `Style`. Settler/notable selection therefore became an unweighted random element of all results, while each furnishing `Generate` call returned all three fixtures, over-furnishing lots and invalidating intended ratios. | Added `Style="pickone"` to all eight intended single-pick tables and a source regression test. Native force-build/arrival sampling must show one result per roll, bounded authored fixture counts, and roughly ordered weights; deterministic semantic callers still require the separate counter-RNG repair above. |
| P1 fixed, native proof open | A yard work declaring `Goods="Yes"` produces a caravan good rather than equilibrium support (`KingdomYardWorks.xml:19-22`; `MODDING.md:782-783`). | `KingdomYardGoods` now projects only exact built-house + matching physical-fixture evidence from the one maintained survey. Pure law pays one dram per due charter cycle, capped at four households; missing, moved, mismatched, duplicate, released, nonworking, or non-Goods evidence adds nothing. `KingdomTrade.PrepareCharterDelivery` freezes adjusted `IncomePerCycle` and `RequestedWater` in the existing durable operation before mutation, so reload or later release cannot reprice/replay it. Parser rejects `Goods` together with `Shades`; Charter prose and public docs disclose rate and cap. | Run TESTING 58a in Qud, including one/five/released/ambiguous fixtures, banked cycles, due-edge cold load, and exact chronicle/ledger totals. Retain save/log evidence before closing native gate. |
| P1 fixed, native proof open | Founder-entered display names are NFC-normalized, trimmed, 1-30 Unicode text elements, reject newline/NUL/C0/C1 and every `Bidi_Control`, and are escaped at rich-text insertion (`_notes/FOUNDATION-CONTRACT.md:916-927`). | `KingdomPresentationRules` now owns new-input admission for every first-city, second-city, covenant, and building-name entry while broad recovery bounds remain separate. Plain names stay plain in identity/transaction state. `KingdomPresentation.Rich` calls Qud's exact `ColorUtility.EscapeFormatting`; rendered reports, Charter/petition/bounty surfaces, chronicles, ledgers, messages, deeds, city books, ceremonies, trade/manifest outboxes, rites, roads, crops, research, raids, succession, building references, and debug reports now escape founder-controlled names only at their Qud presentation boundaries. The source regression walks all production trees and rejects direct realm/seat/exile names at known rendered sinks; the executable test double pins braces, nested controls, `&`, and `^`. The focused presentation suite passes 23 cases. | Run native founding/renaming with combining forms, braces, `&`/`^`, duplicate visible names, 30/31 graphemes, every refused control/bidi class, save/cold-load, Chronicle/ledger/journal/report/quest surfaces, and building references. Confirm formatting is literal, identity values remain plain, and no accepted name rewrites old snapshots. |
| P0 repair in working tree | Roads target authored public/service entrances and mature-city solvers never omit entrances through a small arbitrary plot cap (`_notes/ARCHITECTURE-POLISH-CONTRACT.md:223-228`). | `KingdomRoads.Errands` permanently discarded every home, work, and plot after the first twelve, despite a legal City zone holding 220 works. Its doorway route ignored the immutable architecture receipt and invented one heart-facing door from the rectangle, so an authored side entrance could face a wall while traffic walked elsewhere. | The working-tree repair admits every legal mark/plot to the existing `MaxRoutesPerPass` rotating bounded queue, canonicalizes mark/plot order across reload enumeration, reads every valid frozen architecture receipt, and routes each authored `entrance:public` to its lane. Only receipt-less legacy/staked plots use the geometric fallback; partial receipts fail closed. Add native mature-zone inspection and confirm every public entrance becomes connected without a route-cost spike before closing. |
| P1 | Public behavior APIs require real third-party extensibility across the documented version window and five durable lanes. | Portable tests exercise in-tree fixtures, but the native proof is still explicitly unsigned: TESTING step 124i requires a cold-installed **separate extension assembly** referencing only `ThousandAndFirst.Api`, followed by lifecycle/save tests through 124p. `MODDING.md#standalone-external-fixture-recipe` now gives the exact sibling-mod layout, dependency-gated manifest, lane list, and retained-log procedure. | Build and retain that external fixture mod, cold-install it beside production TAF, execute 124i-124p, and attach exact bytes, logs, and save receipts. |
| P1 code-scope fixed, native proof open | Ordinary construction must route an exact water/material bill from the nearest lawful owned holder when local custody cannot pay; shared route substrate is not permission for remote spending (`VISION.md`, physical-product logistics law). | `KingdomConstructionInput*` and `KingdomCentralLogistics` now own one centrally planned authenticated job spanning exact bill, nearest lawful holders, reserve floors, whole-item/liquid custody, itinerary, carrier, landing, construction funding, rollback/quarantine, recovery, and master pause. Remote stock is never directly debited. | Run final integrated tests and native every-cut cold-save/obstruction/ambiguity/conservation proof. Quote source, route, cargo, destination, and reserve; no cut may mint, substitute, double-charge, or start construction early. |
| P1 fixed, native proof open | The succession brief requires Charter seniority, bounded exact chosen-life configuration with one seat consequence, and the later-activated groomed-designee law. | Realm-bound config v2 and succession save v4 preserve exact resident identity. Chosen-life and its optional seat climb remain distinct from lawful grooming; a bounded grooming record advances only from resident service and city-specific schooling/knowledge-post facts. Charter and death runtime consume both with typed exact-ID fallbacks. Pure/source/portable and staged compile gates pass. | Run expanded Pass 36 through cancel, nominate/replace/revoke, every progress stage, all three cities, save/cold-load, seat exchange, ready accession, each invalidation fallback, and interruption cuts. |
| P1 fixed, native proof open | The reopened assenting moot requires a city-local ephemeral activation gate, durable bounded named consent/exemption, strength from current valid voices, exact native ward ownership/body veto, and fail-closed lifecycle recovery. It must never use NormCore or global faction mutation. | `KingdomReopenedExoticActivation` derives (never learns) the seated key from assent, founder Chavvah, claimed surface/cardinal Moon Stair, and owner-version proof. One exact building and authenticated receipt own six assent/six exemption rows; explicit UI changes them copy-on-write. Exact adjacent `ZoneParts.AmbientStabilization` plus effect ownership and `ApplyAmbientRealityStabilized` body veto project reversibly. Damage/destruction, invalid bodies, strike, secession, thaw/activation/load, duplicate/foreign field, and receipt divergence suspend or quarantine. Ten focused cases pass in both source runners; 1,818 staged sources compile baseline/compatibility. | Execute TESTING 136t-136w in live Qud across all three cities, exact save/cold-load interruption cuts, foreign-field coexistence, effect strength, UI, and visual inspection. Static/source proof cannot sign engine event ordering, serialization behavior, or play feel. |
| P1 code-scope fixed, native/human proof open | Every positive experience/discovery deferral must retain its bounded player-facing purpose without introducing a director, passive chore, remote actor, borrowed body, or invisible loss. | Separate exact owners now implement O0–O11 and D1–D12: common capacity/telemetry, two voices, remembrance, office, locus, witness work, First Guest/First Feast/physical guest, curator/lead, integrated Guest's Feast, site practice, cook lifecycle, decision tag, body history, non-custodial recognition, communal rite, joint view, legacy echo, and explicit vocation services. `docs/V1-EXPERIENCE-UNDEFERRAL.md` is the row-by-row owner ledger. | Run final integrated tests and TESTING Pass 40 native UI/save/accessibility/ablation/quiet-completion protocol. Optional descendants may fail without stranding their semantic parent. |
| P1 code-scope fixed, native proof open | Reopened polity/world presence must provide fresh profile-shaped people, bounded current/rival traffic, caused diplomacy, real custody, and witnessed conflict without unloaded actors, ideology-only war, strategic armies, or offscreen loss. | Typed immutable profile revisions, causal promotion, all seven cohort schedulers, three-city/current-rival dispatch, Trade consignments, loaded hospitality, grievance/terms/truce/intervention, witnessed death/aftermath, consented escrow, W0 capacity, direct records/aggregates/fairness, and exile/return/refound are implemented. Physical endpoints require distinct route-reachable placement and own exact recursive custody, gear/foreign transfer, removal witnesses, fail-closed moved/copied-body handling, `EarlyBeforeDeathRemoval` → `BeforeDestroy` → `OnDestroy` visible-death gates, and cleanup before the sole W0 release. Transaction closure is frozen at 61/61 focused tests and physical narrow checks are green. | Run final integrated tests and TESTING Pass 39 native every-cut custody/death/UI/accessibility/performance/compatibility proof. |
| P1 code-scope fixed, native proof open | A player may prepare visited TAF ground and owned obligations for removal while the mod is still active, but no document may promise cleanup after absent-mod load. | `KingdomRealmRetirement*` freezes exact locators/owners, fences new work, previews and cleans only loaded visited ground plus proved global projections/quests/factions, reports outstanding locators, preserves foreign/player custody, writes an identity fence last, and enforces fresh-incarnation high water. | Run final coverage/integration then TESTING 139m-139o every-phase native cleanup/retry/reinstall proof. Keep mod enabled until successful terminal report; unvisited ground is never remotely loaded. |
| P1 | A released durable schema requires a frozen prior writer/fixture and cold-load round trip. | The engine-owned root currently begins and remains at named version 8 (`Core/KingdomSystem.cs`, serialization constants; `Core/KingdomSystem.z19.PersistenceAndCallbacks.cs`, `Write`/`Read`). TESTING's save-migration harness section accurately records that a frozen native v8 save is required before the first post-public bump. | Freeze the v8 native fixture at release, then require v8→next cold-load/save/reload evidence before any bump. This is an open release-process gate, not permission to synthesize a migration today. |
| P1 line-cap fixed, semantic review open | The catalogue's approved pre-release engineering pass requires services under 300 lines, one responsibility, and protocols at boundaries (`_notes/BUILDING-CATALOGUE-BRIEF.md:417-421`). | Frozen snapshot has 2827 production C# files / 410,378 physical lines: 0 exceed 300 physical lines and 0 fail strict; 0 exceed 1,000, 0 exceed 2,000, and 0 exceed 5,000. Direct XRL imports occur in 1319 files, none over the limit; snapshot digest is `d6c4449c089627a538a7454bedb8e9150e0b3460ed8ec289fe9c19bc03432adc`. Line count cannot prove responsibility/protocol quality; `Tools/check-structure.py --release` still requires exact-inventory human evidence and `docs/STRUCTURE_REVIEW.json` is absent. | Complete human semantic review against this exact digest. Full current-revision native, human, compatibility, performance, and Steam proof remains open. No enterprise-grade or v1.0 claim may bypass either gate. |
| P1 | Architecture release gates require current deterministic goldens plus native gallery, tile-scale overlay review, controller/accessibility, cold-save interruptions, dense-city performance, compatibility, and subscribed-install evidence (`_notes/ARCHITECTURE-POLISH-CONTRACT.md:271-300`). | README and TESTING record the latest retained `19fb8ee` single-city/single-gallery persistence sample and historical two-city smoke. Complete gallery, visual, accessibility, compatibility, dense-performance, numbered-protocol, and subscribed-install evidence remain unsigned. | Retain that sample as a bounded result, then execute the complete native protocol and retain versioned screenshots/logs/save/package evidence. Source/static automation and one native sample cannot sign visual/usability/compatibility claims. |
| P1 fixed, native proof open | Paid construction consumes frozen authored truth; later catalogue merges may shape future work but may not reprice or reclassify a published receipt (`_notes/ARCHITECTURE-POLISH-CONTRACT.md:70-72,118-125`). | Construction registry v4 freezes authenticated `hasPlot`, `frontier`, and final defence truth before any funding for commission, plan, plot, socket-conversion, and improvement routes. Projection, reload, handover, and exact-result checks consume only that receipt. Explicit v1-v3 readers preserve old rows and proof hashes: already-projected legacy work resumes only from exact durable object marks, while an unprojected affected legacy receipt quarantines before another debit or destructive strike because its original catalogue/player-skill truth cannot be reconstructed. Current compact proofs include the immutable fields; contradiction, tampering, override/removal, malformed input, and route coverage are pinned by pure/source tests. | Run the corresponding TESTING construction interruption cuts in Qud, including a paid outstanding defensive plot/frontier work across save/cold-load and a catalogue override/removal. Verify one unchanged classification/defence score, no duplicate debit or projection, and named quarantine for unreconstructable legacy work. |
| P2 fixed | Acceptance must cover the merged catalogue, not a stale numeric subset. | Architecture acceptance and its audit plan pinned 106 entries while TESTING already required the merged data-driven view. | Reworded `_notes/ARCHITECTURE-POLISH-CONTRACT.md:275-276` to every loaded commissionable key and `_notes/BRIEF-IMPLEMENTATION-AUDIT.md:202-204` to the full merged catalogue. |
| P2 fixed | Dissent follows world time and arrests at an awareness brink (`_notes/BUILDING-CATALOGUE-BRIEF.md:380-397`). Production accrues full elapsed days and starts the response window only after warning (`Core/KingdomCreed.02.DissentAndBrink.cs`, symbols `OnZoneActivated` and `RunSecessionWindow`). | TESTING steps 54b/54c incorrectly said dissent moved only while attended and did not move during a season away. | Corrected `TESTING.md:379-385`; added source regression coverage. |
| P2 fixed | Production truth must come from an actual producer; a reservoir stores carried water but does not create it. Runtime null override arrays mean the row's measured carries are live (`Simulation/City/KingdomCityAdvanceable.cs`, especially `WaterRateOf` and `FoodRateOf`; `Simulation/City/KingdomCity.z09.WorksAndAudit.cs:25-39`, written at `Simulation/City/KingdomCity.z02.CheckOut.cs:83-90` and `Simulation/City/KingdomCity.z03.Sightings.cs:53-65`; logical `Simulation/City/KingdomHeartbeat*.cs`, symbol `Advance`). | TESTING step 90q attributed production to a reservoir, while `docs/API.md:948-953` still said per-zone rates were unwired. The prefetch docs also said no checkbox existed although `Options.xml:15` ships one defaulting No. | Step 90q now uses an air-well field plus cistern; API documents measured production rates and default-off checkbox; source regression coverage pins all three. |
| P2 fixed | Current status/backlog must not contradict evidence. | `_notes/README.md` once said nothing had ever run live despite the historical loader result. `_notes/QUESTION-BACKLOG.md:46-47` said quest relabel/marks were absent although accession calls `TryInheritOpenQuests` and applies the marker (`Experience/KingdomSuccession.Accession.cs`, symbol `TryInheritOpenQuests`), with source coverage in `KingdomSuccessionRulesTests.RuntimeInheritsOpenQuestFlavorWithoutChangingQuestState`. | README now distinguishes the narrow retained smoke from the historical two-city smoke and keeps the complete protocol open; backlog records the wired behavior and keeps Pass 36 as the native gate. |

## Supersession checks that are not gaps

- Addendum 10/C10's latest-eligible import choice supersedes Foundation's earlier generic
  player-choice wording; the current default-off option labels that policy. Do not reopen it from
  the older sentence alone.
- The adopted living-city limit is three simultaneous cities: one seat plus two non-seat. A larger
  archival/identity envelope is not proof that live ownership silently promises four.
- Custom petition machinery supersedes Foundation's early base-Quest prototype. Do not demand two
  competing quest authorities.
- `_notes/EXPERIENCE-SYSTEMS-RESEARCH.md` is evidence, not runtime authority. Its candidate mechanics
  do not become shipment promises without adoption in a binding brief/decision.

## Repairs and verification scope

The initial static pass changed protocol/contract prose and source tests only. Subsequent bounded
repairs now cover raid lifecycle, exact distance/central logistics, carry-sign v6, citizenship
overlay, elapsed option latches, master pause, civic wrappers, physical happenings, petitions,
authored architecture, and the maintained survey indexes described in their rows. Each repair is
still judged independently: code/content and portable proof do not close its named native gate.

Current checks at the 2026-09-01 source freeze:

- `python3 Tools/generate-lot-realizations.py --check`: current, 146 maps / 107 bindings / 122 tiers
  and 527 creed fixtures.
- Staging/ABI inventories: the frozen source snapshot has 2827 production C# files and
  2,857 cold-install files. Its 410,378 physical lines have 0 exceed 300 physical lines, none exactly
  300, therefore 0 fail strict; 0 exceed 1,000, 0 exceed 2,000, and 0 exceed 5,000. Direct XRL
  imports occur in 1319 files, none over the limit; snapshot SHA-256 is
  `d6c4449c089627a538a7454bedb8e9150e0b3460ed8ec289fe9c19bc03432adc`. Any source change invalidates
  this digest; `docs/STRUCTURE_REVIEW.json` human review remains missing. Art policy has 0 local allowlisted tiles / 125 verified
  vanilla paths.
- `python3 Tools/check-architecture.py`: PASS, 144 buildings, 134 plot buildings, 89 palettes,
  333 maps (187 source / 146 generated), 220 plans, 226 bindings, 262 tiers, 344 variants, and
  1,376 goldens, zero issues or warnings. Benefit-provider content is clean at 114 catalogue rows,
  187 authored variants, and 105 unique fixtures.
- `Tools/gate.sh`: baseline, compatibility, and development lanes are clean.
- Last retained managed suites pass 10,624/10,624 Qud-referenced/source and 2,325/2,325 portable but require a final current-source rerun;
  current documentation/static suites pass 300/300 Tools and 28/28 Art. Historical
  7,743/173/35/23 receipts remain bound only to their old bytes.
- Native evidence remains frozen at clean commit `19fb8ee`: founding, one of 2,064 gallery cases,
  save/cold-load, repeated self-tests, and clean log. Later structural revisions need a native
  compile/load/log rerun against their exact final commit. Full native, human, compatibility, dense-performance,
  semantic-review, and Steam gates remain open. `docs/STATUS.md` owns the latest concise evidence
  snapshot. No row here claims live Qud closure.
