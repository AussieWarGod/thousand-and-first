# Recruitment structural review

Automated source review at `807a89fcb23f8aef7a677dd162569d9d6115425b`, compared with dev
`ec29b4cd514b47f2f0297d1fa2a9016e80460a23`. All 3,112 production paths compared:
3,102 byte-identical, six changed, four added, none removed. Census: 441,029 physical lines,
1,454 direct XRL imports, zero files at or above 300 lines. Inventory SHA-256:
`e9e71e2de55a777def5a6ddb283ab4b493b2e555ae2991b0543c668296017aa4`.

| Responsibility | Review |
| --- | --- |
| `KingdomRecruitmentRules` | Pure bounded arithmetic accepts native hostility from the adapter and applies both reputation channels without engine authority. |
| `KingdomRecruitment` | Validates the merged one-body catalogue, native allegiance and explicit identity metadata; reads personal/inbound city standing and returns selection or a bounded failure. Status follows configured story policy without publishing an option epoch. |
| `KingdomRecruitment.Names` | Generates native culture/species/faction names from a separate deterministic draw without allocating sample bodies; restores native random references in `finally`. |
| `KingdomSemanticSelection.Recruits` and callers | One payload builder retains event identity, creed draw and date; existing frozen-plan and historical recovery paths remain separate. |
| `KingdomGrowth.ArrivalCadence` | Only the exact no-eligible sentinel takes the valid waiting path, retaining unspent debt and allowing downstream work. Other malformed catalogue/authority failures remain faults. |
| Identity allowlist and removal coverage | Three ordinary native profiles extend the existing owned-body list and generated removal metadata; no new membership authority. |

XML review covers source faction/origin tags, bounded base weights and native inheritance for
Issachari, hindren and dromad. These retain anatomy and culture while removing inherited village
conversation; ordinary Dromad is not a merchant/restocker. First guests retain their explicit
allowlist; ordinary extensions must satisfy the catalogue contract. No serialized field, wire
format or public extension signature changes. The new metadata requirement is documented in
HOUSING-AND-POPULATION.md and the population XML.

Native selection, policy, hostile waiting and recovery evidence is scoped in STATUS.md.
Actual admission/cold-load validation remains separate, as does population balance or multi-map
acceptance. Full comparison and census are retained in local archive
`recruitment/807a89fc/structural-review-1/result.json`.
