# Absent-owner home-loss structural review

Automated delta review against `149daf47`: 3121 staged production paths compared,
3109 identical, 10 changed and 2 added. Full per-path hashes and census:
`home-map-damage/home-loss-review-20260915/`. Inventory SHA-256: `5662178c2dd8f60bc010089a6130b5156247ea4c83f6911a43c3c3db7d024587`.
All production files remain strictly under 300 physical lines; total 441,657 lines.

| Responsibility | Reviewed boundary |
| --- | --- |
| Frozen roof claims | Optional home-map authority in `sr3`; work/plot and resident/body identity retained. Old `sr1`/`sr2` decode with no new authority, preserving local-body obligations and canonical wire checks. |
| Native capture | Canonical city owners are selected independently of body location. Exact binding and available body checks remain. Legacy named local owners use the old basis; unknown absent owners refuse capture rather than disappear. Unnamed rows retain the existing no-brink policy. No remote map loading. |
| Native application | Home ownership and body binding are rechecked. An unavailable remote body is admitted only for a canonical home claim; locally bound or legacy recipients still require their exact live body. Global duplicate-body checks remain. |
| Publication | Existing typed roof compare-and-swap also compares residence value/list identity. Only the frozen roof intent can bypass competing-writer fences; whole-book projection stays blocked during settlement. |
| Ordinary home loss | Existing resident brink storage records absent canonical owners. Observed loss preserves any prior dated/warned record. No new warning deadline is started. Reconciliation publishes home clearing and newly observed loss together. |

15,068 main / 5,954 portable cases pass, zero skips, including mixed claims, loss-clock replay,
stale carriers and home changes. All four engine compile modes pass. Native damage/application/cold recovery
are separate gates; native acceptance remains pending. This review is automated, not a human
sign-off or all-building Beta acceptance. The preceding residence review retains its prior scope.
