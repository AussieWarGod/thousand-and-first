# Completed first heart and claimed-zone sight after cold load

Status: implemented; native execution pending. This is developer evidence, not a claim that the
first heart is enclosed or that the sight fix has reached the public Workshop item.

The exact `heart-sight-lifecycle-native-check.persona` starts genuine marsh Quickstart with the
advisor, observes its founding receipt and initial canvas count, and advances 8400 ordinary
turns. Existing lifecycle assertions require surviving housed founders and the completed paid
fire. The heart observer requires the original founding receipt, settled terminal effects,
exact final object custody and predecessor removal proof. It does not force a future deadline.

The shared read-only realized-architecture capture validates every receipted component. An
independent coordinate set then requires all seven canvas walls, including the three flank
cells outside the frozen 4x4 footprint. Live solidity and a drawable tile or glyph are required.
The 6x4 camp is authored open to the south, with an open roof: this test records `enclosed=false`.
It does not turn an open camp into evidence of an enclosed tent.

Three real source frames must draw all 2000 local-zone cells as visible and lit, including
cells naturally hidden by line of sight. Every frame must restore native gameplay visibility.
The test stores a developer witness containing the realized digest, physical canvas identities
and coordinates, frozen layout and founding/terminal receipts, then performs a real save.

A separate sealed process compares that witness before AfterGameLoaded and after activation,
then runs the existing paid next-commission assertion. Only this exact scenario prepares an
explicit Continue result. The load barrier releases it after successful worker completion and
cleanup; failures and all existing terminal load scenarios remain parked. Vanilla `RunGame`
then draws real frames. The saved autorunner's `HasConsideredScript` must already be true and
remain true, preventing the source script from replaying. No running flag, visibility map or
world data is repaired. `Session:false` avoids introducing backup files into the sealed profile.
Completion is journaled only after the loaded loop draws and restores at least three frames
and the same physical heart still compares exactly.

## Execution

Use a clean committed source tree and the persona's exact `REQUEST`, `SCRIPT`, and `VERBS`.
Prepare with seed `#43101`, `TAF_SCENARIO_QUICKSTART_ADVISOR=yes`, role `save-session`,
turn budget 10000 and timeout 1200. Use the existing owned launcher, terminal journal,
receipt-owned stop and stop record. Assert the source persona, import the stopped save with
`Tools/prepare-scenario-load.py`, then run and stop that separate owned load process.

Require both full profile seals, exact source binding for all game/harness inputs, strict
Player.log checks and both journal oracles:

```bash
python3 Tools/check-quickstart-lifecycle.py SAVE_JOURNAL LOAD_JOURNAL \
  --results LIFECYCLE_RESULT --run-record SAVE_RECORD,LOAD_RECORD
python3 Tools/heart_sight_check.py SAVE_JOURNAL LOAD_JOURNAL --results HEART_SIGHT_RESULT
```

Retain failures unchanged. A source-only pass does not establish cold-load rendering, and
passing journal text does not substitute for source binding, profile seals or strict logs.
