# Quickstart recovery candidate: structural review

Reviewer: Codex root, automated source review. No independent reviewer or human
signature is claimed. This review concerns structure and boundary protocols;
native behavior and release acceptance require their own evidence.

## Exact inventory

Reviewed source at `0d310b5e21568b4e98cae7a7445f7def6955d5d9`, including production
changes through `bdde5cff286b96b2aab194830604445c9ec38a63`.
Staged C# inventory SHA-256:
`edb507bb9f3fc5203e4fe3f83312f4c4c9adda7c9fbba99e6b2a73145a212205`.
3098 files, 440079 physical lines, 1445 direct-XRL files; every file is strictly
under 300 physical lines. No exception is requested.

Every staged C# path and source blob was compared with public main
`e96b50e1c7ce698ed05749963d4fb885ab42d054`: 3003 unchanged, 66 modified,
29 added, no C# removal. Local comparison artifact:
`/home/r/work/taf-scratch/hotfix-quickstart.FIuLCD/structural-delta.json`.
Unchanged bytes inherit the public commit's review chain and its stated limits;
they were enumerated and compared, not all newly deep-read. Modified/new source
was read by responsibility group, including relevant existing callers and guards.

## Delta responsibilities and boundaries

| Group | Review |
| --- | --- |
| Founder receipt, bootstrap and origin accounting | Core owns canonical receipt decisions and codecs; World owns engine reads and writes. Four unique identities are frozen before enrollment. Raising is durable before factories; uncertain partial cohorts are quarantined without replacement. Origin accounting binds body/profile/city and refuses ambiguous credit. Old q1/q2 receipts remain omitted and terminal; ordinary load returns before reseeding or re-verifying departed founders. No new serialized IPart field. |
| Deposit engine and physical census | Raw observations use bounded long accumulation and explicit unknown results. An unknown destination cannot mint credit; only the delivery's still-private parcel may be discarded. After insertion, unknown credit does not authorize deleting destination contents. Earlier proved deliveries remain credited. Raw malformed stack counts are distinct from occupancy readings. |
| Authored component census and upgrades | Pure membership rules distinguish current generation, one exact mapped peer, and foreign candidates. Runtime resolves peer identity, cell, token and retain state per call. Exact blueprint, placement, receipts and unique identity checks remain. Removal stays a separate responsibility with the original global aftermath proof. Token preimage is unchanged. |
| Construction dispatch and final output | Recovered committed inputs admit the current valid job through later phases instead of stranding it at Funded. The existing resume machine still chooses the action. A freshly created final building receives its engine identity before publication; existing objects are read without replacement. Failed publication remains a refusal with a named diagnostic. |
| Plot siting and occupants | Candidate filtering uses claimed cells and placements from the resolved layout. Ground application separates structural passability from bodies. Clearance plans exact resident/beast moves to distinct passable ground, refuses player/companion/foreign residents, checks post mobility, and attempts rollback on failed displacement. Improvement clearance occurs after funding; strict envelope proof still follows. Callback mutation and failed rollback are not claimed harmless or completely covered by this structural review. |
| Founding heart and rung effects | The founding terminal remains a historical first-generation witness. Completed improvement receipts plus successor removal/custody evidence provide the additional root binding; position alone never chooses a successor. Unfinished replacement has typed Pending classification. Shared rung effects preserve at-most-once ceremony and re-prove route-specific endpoints after ceremony and basin callbacks. Adjacent rung tiers use the authored mapping, including equal final footprints. Further successive-rung native coverage remains separate. |
| Seal capture and clocks | Captured, Unavailable, Malformed and Pending remain distinct. Both load branches tolerate only typed Pending without publishing a new seal or advancing revision. Exterior blockage becomes Pending only after component and internal passability verification; missing entrances remain structural refusals. Master resume reanchors a completed semantic receipt, while unfinished/malformed active receipts keep their prior dispatch tick. |
| Quickstart resources, guide and departure admission | New worlds receive 48 finite drams and plain founder/shelter/guest instructions. Departure admission recounts the owning settlement after pending/orphan recovery and retains the last two citizens. The existing final emigration floor remains. This is voluntary-departure protection, not protection against death or arbitrary external mutation. |
| Generated removal inventory | Compared extracted literals: ten additions, no removal. Additions cover new origin/announcement properties and existing campfire/heart-stockpile blueprints. Generated inventory retains its existing responsibility. |

The additions fit their existing domain boundaries; no file-size exception or
new cross-domain serialization mechanism was introduced. No blocking structural
finding remained in the reviewed delta. This is a risk-weighted automated review,
not an exhaustive proof against engine callbacks or a new review of 3003 inherited
files.

## Separate evidence and remaining limits

Canonical compile passed all four modes at this production inventory. Main and
portable suites passed 14772 and 5732 cases with zero skips. Logs:
`/tmp/taf-hotfix-water-ingress-gate.log`,
`/tmp/taf-hotfix-water-ingress-full.log`,
`/tmp/taf-hotfix-water-ingress-portable.log`.
These checks support compilation and tested contracts; they do not replace
native gameplay evidence.

The final Quickstart location/guide matrix is recorded separately under
`/home/r/work/taf-scratch/hotfix-quickstart.FIuLCD/final-matrix-1`.
The earlier successful housing stress run belongs to `e1d26d9`, not this inventory.
Existing-save recovery, cold load after founder departures, further heart rungs,
other Beta feature combinations and subscribed delivery are not signed by this
review. See `QUICKSTART-HOTFIX-PROGRESS.md` and the coverage matrix for their actual
status.

## Full-cask and strike follow-up review

The preceding review remains historical evidence for `edb507bb9f3fc5203e4fe3f83312f4c4c9adda7c9fbba99e6b2a73145a212205`.
The follow-up staged inventory is
`43ae0c005513626488ce557061ad506898e9c6c26c3bd935639db5ecfff307b6`:
3098 files, 440089 physical lines, 1445 direct-XRL files, no file at or above
300 lines. Production changes since that review were read directly:

- Quickstart's finite water grant fills the existing 64-dram vessel. The guide
  names that quantity, and grant verification derives its diagnostic from the
  same constant. No new container, refill-on-load or historical receipt migration.
- Strike orders may move from Published to Working only with the same already
  stamped StrikeWorking phase and exact funding on both records. Registry identity,
  revision, route and input guards still apply. Other routes and unpaid or unstamped
  jobs retain their refusals. This admits the interrupted production strike path
  without broadening purchase transitions. Three automated tests cover reload and
  terminal progression, all other routes/phases, and unpaid or changed stamps.

The housing save/load extension is harness-only. It freezes original survivor and
departed IDs before a real save, then checks those identities and usable housing
after cold load without restoring departed founders. Its full native result is
 separate; compilation alone does not prove the load behavior.

The 48-dram nine-day matrix passed marsh/dunes with both guide settings. Canyon
 correctly failed an unfinished 15000-tick rocky-ground fire quote; its log shows
 the four founders already housed. The new fourteen-day matrix and full-cask native
 housing recovery remain pending. The source review does not upgrade those results.

## Automatic site protection follow-up

Current production inventory:
`9a35be8ed58f0767e43c0e30d6f286590c6c0da93dffb36d9303eec51c96329f`.
3098 C# files, 440099 physical lines, 1445 direct-XRL files, no size exception.
The only production changes since the full-cask/strike review are the shared
connection census becoming internal and the plot-selection caller using that census
plus live stair parts. Both enumerated and cached connections are included. Candidates
are rejected only where resolved claimed cells or placements cover protected cells;
unclaimed margins remain eligible. Final preflight still independently rejects stairs,
connections, invalid mappings and other protected ground. No new mutation or receipt
format is introduced. Source review read both changed files and final preflight.

Main/portable suites passed 14775/5732 cases, zero skips, and all four canonical
compile modes passed. Logs: `/tmp/taf-stair-siting-full.log`,
`/tmp/taf-stair-siting-portable.log`, `/tmp/taf-stair-siting-gate.log`.
The native canyon source reached fourteen days, four housed citizens, a completed
paid building and a real save on the predecessor. Cold load preserved those facts;
its next commission refused a connection at 29,12 that site selection had missed.
That complete chain remains FAIL. Native verification of the selection fix is pending.
