namespace ThousandAndFirst
{
	/// <summary>Ephemeral resolved owner for one already-bounded endpoint due row.</summary>
	public sealed class KingdomPolityTrafficAssignment
	{
		public KingdomPolityDueWork Work;
		public string PolityId;
		public string RelationId;
		public bool External;
		public string CauseDigest;
	}
}
